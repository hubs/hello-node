#circle.yml
machine:
    #node:
    #   version: 0.12.7

    #change run on docker
    services:
        - docker

 dependencies:
     override:
        #docker build
        - docker build -t hello-node .       
test:
      override:
          - ./node_modules/.bin/mocha
          - docker run -d -p 3000:3000 hello-node ;sleep 10
          - curl --retry 10 --retry-delay 5 -v http://localhost:3000